numpy==2.3.3
pandas==3.0.2
pyarrow==25.0.1
pytest==9.0.2
requests==2.32.5

# BLOQUEADO: el codigo importa `anthropic`, pero el paquete no esta instalado
# en esta maquina. No se fija una version inventada; G1 debe seguir bloqueada
# hasta instalarla y registrar la version que devuelva `pip freeze`.
