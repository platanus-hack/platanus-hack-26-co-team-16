# Auditoría Peeky — 2026-08-22 12:48 · alcance repo

> Informe del agente `peeky`. Coherencia interna; no es evaluación de negocio ni de hackathon.
> **Material:** repo completo (árbol de trabajo)
> **Commit:** `158f689` · **Rama:** `rol/datos`
> **Cross-modelo:** ejecutado (`mcp__codex__codex`, read-only) — 13 candidatos en un lote: **13 CONFIRMADOS, 0 refutados**
> **Hallazgos:** 13 · 🔴 0 · 🟠 5 · 🟡 8

## 1 · Hallazgos inconsistentes

Sin informes previos de `peeky`: no hay delta. `juez-tecnico` publicó a las 12:38 y **siguen abiertos** sus cinco (fórmula `min()` vs ADR 0007 · el `seed` que no siembra nada + caché sin versionar · base 30,6% vs 55-60% · presupuesto no atómico · cero manifiestos). No los repito.

| Sev | Archivo:línea | Lo que vi | La inconsistencia | Tríada | Dueño | Ev. | codex |
|---|---|---|---|---|---|---|---|
| 🟠 | `behavior/contrato.py:48` | `FALLBACK = "absorber"` | `context.md:94-97` y `engine/MODELO.md:26` fijan la estrategia terminal en **`cumplir`**. Vía `contrato.py:209-211` un arquetipo informal que cae en `absorber` cuenta 1,0 fuera de regla; con `cumplir`, 0,0: el nombre mueve la cifra del pitch | CÓMO | Nico ↔ Manuel | [disco] | ✅ |
| 🟠 | `behavior/contrato.py:135` | `decision_fallback()` escribe `"veto": {"factible": True}` | `contrato.py:10`: *"Manuel llena `veto`. Ninguno de los dos toca el campo del otro"*. El fallback entra al agregado con un veredicto que el motor nunca emitió | CÓMO | Nico | [disco] | ✅ |
| 🟠 | `behavior/contrato.py:98` | `cargar_contrato()` sin llamadores | `validar()` (`:140`) comprueba la tupla hardcodeada `CAMPOS_MIOS` (`:32`). **Ningún `.py` abre `contracts/decision.json`**: el contrato congelado no tiene ejecutor y cambiarlo no rompe nada | PARA QUÉ | Nico ↔ Alejo | [disco] | ✅ |
| 🟠 | `contracts/ronda.json:2-9` | 8 campos, ninguno con desglose de estrategias ni corte por sector | `web/README.md:9-13` fija A3 y A4 como elementos 3 y 4 de la interfaz y `:23` manda construirlos contra ese contrato. `desglose_estrategias()` (`rondas.py:69`) no sale por `a_contrato()` (`:53-67`): su único consumidor es `demo.py:63` | CÓMO | Alejo (con Manuel, Dani) | [disco] | ✅ |
| 🟠 | `README.md:41` | *"el control de contaminación… vive en `Politica.como_mecanica()`"*, en presente | `git ls-files engine/` → solo dos `.md`; `engine/MODELO.md:21` la planea en `mundo.py`. La guardia real, `behavior/higiene.py`, el README no la nombra | PARA QUÉ | Juanda | [disco]+[pendiente] | ✅ |
| 🟡 | `behavior/capa.py:182,193,198` | Sale `intento`, `fallos_tecnicos`, `familia`, más `fue_fallback` (`contrato.py:136`) | `contracts/decision.json:2-7` declara 6 claves. Cuatro campos que el contrato congelado no menciona; `familia` es el que la pantalla necesita | CÓMO | Nico ↔ Alejo | [disco] | ✅ |
| 🟡 | `behavior/higiene.py:128` | `escanear_prompts()` solo lo dispara su `__main__` | No está en el `Makefile` ni en `.github/` (solo `pull_request_template.md`; no hay CI). `verificar()` sí protege en runtime; el escaneo previo es la red que nadie tensó | PARA QUÉ | Juanda (con Nico) | [disco] | ✅ |
| 🟡 | `behavior/cache.py:12-15` | *"el determinismo… así se reporta en `VALIDATION.md`"* | `VALIDATION.md` no tiene una sola aparición de "cach" ni "determinis". El documento designado para declarar la frontera del ADR 0009 no la declara | CÓMO | Nico ↔ Juanda | [disco] | ✅ |
| 🟡 | `behavior/cliente.py:107`, `ablacion.py:39` | `contexto` como parámetro de API pública, cargando el agregado (`capa.py:162-172`) | `context.md:73-77` define **Agregado** y prohíbe por nombre *"contexto"*. El docstring no viaja con la firma cuando `engine/` la importe | QUÉ | Nico | [disco] | ✅ |

Los cuatro restantes, todos ✅: **`cliente.py:145-147`** registra el gasto antes de escribir el caché, así que la llamada que agota el tope se paga y se pierde (🟡, Nico) · **`README.md:80-88`** marca los cuatro pasos de entrega con ✅ mientras `platanus-hack-project.jsonc:3-15` tiene cuatro `<FILL THIS>` y `project-description.md:1` sigue siendo plantilla (🟡, Juanda) · **`.gitignore:23`** ignora `behavior/cache/`, ruta que nadie usa —el caché vive en `behavior/.cache` (`cache.py:30`)—, así que quien lo versione por el ADR 0009 editará la línea inerte (🟡, Juanda) · **`contrato.py:37`** `ESTRATEGIAS_CONOCIDAS` sin lector: sus dos propósitos los cubren `familia()` y `FALLBACK` (🟡, Nico).

## 2 · El interrogatorio de grounding

**Nico** — `rondas.py:249`: `converge()` es función pública con el término que el glosario prohíbe (`context.md:34`), y el docstring que la desactiva no viaja con el nombre cuando la importen. ¿Qué pasa el día que `make validate` imprima "converge: True"? Sin responder: **QUÉ**.

**Manuel** — `engine/MODELO.md:28` declara `muestrear(arq, n, rng)` dentro de `engine/`; `behavior/arquetipos.py:175` ya tiene `muestrear(distribucion, n, seed, …)` con otra firma. ¿Cuál sobrevive, quién la llama y en qué carpeta queda? **CÓMO**.

**Juanda** — el "informe de honestidad" se publica con tres comandos que dan números distintos: `VALIDATION.md:80` (65, mayoría prosa), `AGENTS.md` (`--include=*.py` → 21) y `Makefile:82` (21). ¿Cuál es el número que se dice en el pitch? **QUÉ**.

**Dani** — `git ls-files web/` devuelve solo `README.md`, `ROLES.md:88` fijaba el Next.js desplegado en H+4 y `platanus-hack-project.jsonc:15` tiene `deploy-url` en `<FILL THIS>`. ¿Hay hoy alguna URL que abra sin sesión, aunque sea fea? **PARA QUÉ**.

## 3 · Cabos sueltos que arruinan la demo

1. **Salida: no hay dónde mostrarla.** Momento: el jurado abre la página de votación. Disparador: `platanus-hack-project.jsonc:15` en `<FILL THIS>` y `web/` sin código. Síntoma: la tarjeta sale sin link. `[disco]`. Cierra: Juanda con Dani. El pendiente está declarado en `handoff.md:28-33`; lo que agrego es que `README.md:80-88` ya lo da por hecho.
2. **Instalar y ejecutar** (sin manifiesto, `make` ausente en Windows) y **llamar a la API** (sin caché versionada → `SinCredenciales`, `cliente.py:126`): **ya publicados por `juez-tecnico`**, siguen abiertos. La ruta de ablación (`python -m behavior.demo`) sí corre a $0 sin credenciales.

## 4 · Veredicto de coherencia

El flujo punta a punta no existe en disco, pero eso está declarado y no es lo que reporto. Reporto que las costuras entre lo que sí existe no son costuras: son documentos que parecen compatibles. Los tres contratos están congelados y **ninguno tiene ejecutor** —`cargar_contrato()` no se llama, `validar()` comprueba una tupla, el código emite cinco campos no declarados, y dos de los cuatro elementos de la interfaz no tienen campo por dónde llegar—. La estrategia terminal se llama `cumplir` en el glosario y en `engine/MODELO.md`, y `absorber` en el único código que la implementa, con efecto directo sobre la tasa de informalidad. La capa que no debe tocar el veto lo escribe en su ruta de fallback. Las restricciones no-negociables están implementadas donde alguien las ejecuta y declaradas donde nadie las dispara: `higiene.verificar()` protege cada llamada de verdad, pero su escáner previo no lo corre ni el `Makefile` ni un CI inexistente, y el README señala la guardia de contaminación a una función no escrita mientras la que existe no se nombra. No es un collage: hay dirección y está documentada. Es un producto cuyas dos mitades escritas se describen mutuamente con documentos, no con código que se llame.
